// Imports =================================================================
import css from './Dialogs.module.css'

// Dialogs Component ========================================================
const Dialogs = () => {
    return <section className={css.dialogs}>
        Dialogs
    </section>
}
export default Dialogs