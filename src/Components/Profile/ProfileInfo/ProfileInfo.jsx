// Imports =================================================================
import css from './ProfileInfo.module.css'

// Profile Info Component =====================================================
const ProfileInfo = () => {
    return <section className={css.profileInfo}>
        Wallpaper + Ava + Description
    </section>
}

export default ProfileInfo